/**
 * Physical measurement topology (stub).
 *
 * Planned: buildings, apartments, rooms, fuse boxes, meters, and effective
 * fuse-box-to-apartment / meter-to-fuse-box assignments (spec REQ-TOP-001,
 * tasks T005). Not yet implemented — the original Python lane was also a stub.
 */
export {};

/**
 * Health, readiness, and observability (stub).
 *
 * Planned: liveness/readiness and structured metrics for ingestion, calculation
 * duration, coverage, quarantines, stale boards, and jobs (spec REQ-OPS-001,
 * tasks T022). A minimal /api/health endpoint is wired directly in app.ts.
 */
export {};
